name: marvel_heroes
description: Heroes from Marvel Comics in one app

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# Read more about versioning at semver.org.
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^0.1.2
  http: ^0.11.3
  crypto: ^2.0.6
  cached_network_image: ^0.5.0
  flutter_staggered_grid_view: "^0.2.3"
  sensors: ^0.3.5
  palette_generator: ^0.1.1
  random_color: ^1.0.3
  carousel_slider: ^1.0.1
  flutter_custom_tabs: ^0.4.0


dev_dependencies:
  flutter_test:
    sdk: flutter


# For information on the generic Dart part of this file, see the
# following page: https://www.dartlang.org/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
     - assets/landscape.png
     - images/loadingHeroes.gif
     - images/bigLoadingHeroes.gif
  # To add assets to your application, add an assets section, like this:
  # assets:
  #  - images/a_dot_burr.jpeg
  #  - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.io/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.io/assets-and-images/#from-packages
  fonts:
    - family: Marvel
      fonts:
        - asset: fonts/Marvel-Regular.ttf
        - asset: fonts/Marvel-Italic.ttf
          style: italic
        - asset: fonts/Marvel-Bold.ttf
          weight: 700
        - asset: fonts/Marvel-BoldItalic.ttf
          weight: 700
          style: italic
    - family: FF
      fonts:
      - asset: fonts/FF.ttf
    - family: Black
      fonts:
      - asset: fonts/blackFont.ttf
